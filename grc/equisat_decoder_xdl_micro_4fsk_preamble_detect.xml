<?xml version="1.0"?>
<block>
  <name>xdl_micro_4fsk_preamble_detect</name>
  <key>equisat_decoder_xdl_micro_4fsk_preamble_detect</key>
  <category>[equisat_decoder]</category>
  <import>import equisat_decoder</import>
  <make>equisat_decoder.xdl_micro_4fsk_preamble_detect($num_blocks,$max_symbol_ratio)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Num blocks</name>
    <key>num_blocks</key>
    <value>15</value>
    <type>int</type>
  </param>

  <param>
    <name>Max symbol ratio</name>
    <key>max_symbol_ratio</key>
    <value>0.33</value>
    <type>float</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>float</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>message</type>
  </source>
</block>
