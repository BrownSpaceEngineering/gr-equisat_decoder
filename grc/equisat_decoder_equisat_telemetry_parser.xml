<?xml version="1.0"?>
<block>
  <name>EQUiSat Telemetry Parser</name>
  <key>equisat_decoder_equisat_telemetry_parser</key>
  <category>[EQUiSat Decoder]</category>
  <import>import equisat_decoder</import>
  <make>equisat_decoder.equisat_telemetry_parser()</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>message</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
</block>
